using UnityEngine;
using System.Collections;

public class SetHelmetPos : MonoBehaviour {

	public Vector3 newPos;

	// Use this for initialization
	void Start () 
	{
		//this.transform.position = gameObject.transform.position + newPos;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
